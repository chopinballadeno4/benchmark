tool:
    infer

command:
    infer run
        --biabduction
        --bufferoverrun
        --cost
        --liveness
        --loop-hoisting
        --parameter-not-null-checked
        --pulse
        --racerd
        --siof
        --self-in-block
        --starvation
        --keep-going
        -- clang++ -c main.cpp

expected result:
    no issue.

actual result:
    issue. Infer overestimates the return value of a loop-based length function, leading to a false buffer overrun.